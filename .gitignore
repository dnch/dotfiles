# Don't commit machine-specific Fish configuration
fish/config.local.fish
fish/fish_variables

# anything in `var` has been installed via a
# package manager (e.g. fisher); don't commit it
var

# Don't commit Fisher, except for the fishfile
!var/fisher/fishfile
fish/functions/fisher.fish
fisher

# Never commit node_modules
node_modules

# duh
tmp

# all the crap we don't want to track
configstore
irb
gh
iterm2
yarn

